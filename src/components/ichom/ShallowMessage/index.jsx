export default function ShallowMessage({ count, kind }) {
  return (
    <div className="empty">
      <div className="big">
        {count} {kind}
      </div>
      Full structure is in the ICHOM ingestion pipeline. Six Sets currently have
      full variable detail attached — open one of those (Type 2 Diabetes, CAD,
      Hip & Knee OA, Pregnancy, Depression & Anxiety, or Localised Prostate
      Cancer) to see the complete variable schema.
    </div>
  );
}
